describe('Create Category', () => {
  it('2 + 2 should be 4', () => {
    const sum = 2 + 2;
    const result = 4;

    expect(sum).toBe(result);
  });

  it('2 + 2 should not be 5', () => {
    const sum = 2 + 2;
    const result = 5;

    expect(sum).not.toBe(result);
  });
});
